#include<iostream>
#include<cmath>
using namespace std;
float poisitveQuad();
float negativeQuad();
main()
{
int a=5;
int b=6;
int c=1;
quad1=poisitveQuad();
quad2=negativeQuad();
}
float negativeQuad()
{
  int power=pow(b,2);
  int 	mult=(4*a*c*);
  int subtr=power-mult;  
int square=sqrt(subtr);
  int num=-b+square;
   int quad1=num/2*a;	
   return quad1;
}
float poisitveQuad()
{
{
  int power=pow(b,2);
  int 	mult=(4*a*c*);
  int add=power+mult;  
int square=sqrt(add);
  int num=-b+square;
   int quad1=num/2*a;	
   return quad1;
}